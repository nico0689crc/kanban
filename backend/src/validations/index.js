const userValidations = require('./user');
const projectValidations = require('./project');
const taskValidations = require('./task');
const sectionValidations = require('./section');

module.exports = {
  userValidations,
  projectValidations,
  taskValidations,
  sectionValidations,
};
